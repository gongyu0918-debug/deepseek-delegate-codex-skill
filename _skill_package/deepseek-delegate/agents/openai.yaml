interface:
  display_name: "DeepSeek Delegate"
  short_description: "Pro-only packet-local advisory review through DeepSeek TUI CLI"
  default_prompt: "Use $deepseek-delegate only for a bounded packet, request --structured-result, keep deepseek-v4-pro as the delegate model, inspect the JSON envelope, and verify findings locally."
